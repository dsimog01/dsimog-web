name: Build and push image to GHCR

on:
  workflow_dispatch:
    inputs:
      version:
        description: "Image version (tag)"
        required: true

jobs:
  push-to-ghcr:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v3

      - name: Login to GHCR
        uses: docker/login-action@v2
        with:
          registry: ghcr.io
          username: ${{ github.repository_owner }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push React TS app
        uses: docker/build-push-action@v3
        with:
          context: "."
          file: ./Dockerfile
          push: true
          tags: ghcr.io/${{ github.repository_owner }}/dsimog-web:${{ github.event.inputs.version }}
